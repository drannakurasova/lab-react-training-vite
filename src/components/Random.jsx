function Random (props) {
  console.log (typeof props.max)

    return (
        
        <h3>Random value between {props.min} and {props.max} is `${randomNumber
        = (min,max) => {
    return Math.floor(Math.random *(() {props.max}-{props.min}) + {props.min})  }
        
        }`
        </h3>

    )
}
export default Random